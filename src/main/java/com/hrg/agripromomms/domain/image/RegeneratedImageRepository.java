package com.hrg.agripromomms.domain.image;

import org.springframework.data.jpa.repository.JpaRepository;

/** regenerated_image 테이블 저장소입니다. */
public interface RegeneratedImageRepository extends JpaRepository<RegeneratedImage, Long> {
}
